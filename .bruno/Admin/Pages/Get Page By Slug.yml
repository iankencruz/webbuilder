info:
  name: Get Page By Slug
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/pages/:slug
  params:
    - name: slug
      value: page
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
