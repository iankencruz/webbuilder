info:
  name: Get Resolved Blocks
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/pages/:id/blocks/resolved
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
