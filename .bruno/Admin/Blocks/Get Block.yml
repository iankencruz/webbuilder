info:
  name: Get Block
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/blocks/:collection/:id
  params:
    - name: id
      value: "3"
      type: path
    - name: collection
      value: hero
      type: path
  body:
    type: json
    data: |-
      {
        "heading": "Welcome to my page",
        "subheading": "A great subheading",
        "cta_label": "Get Started",
        "cta_url": "/contact"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
