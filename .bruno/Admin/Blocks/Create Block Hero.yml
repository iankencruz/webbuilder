info:
  name: Create Block Hero
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/blocks/hero
  body:
    type: json
    data: |-
      {
        "content": "Json richtext",
        "format": "html"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
