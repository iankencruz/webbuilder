info:
  name: List Blocks
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/blocks/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
