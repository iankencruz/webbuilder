info:
  name: Delete Page
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/pages/:slug
  params:
    - name: slug
      value: page
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
